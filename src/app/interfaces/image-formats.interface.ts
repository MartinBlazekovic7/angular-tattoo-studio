import { ImageFormat } from '@interface/image-format.interface';

export interface ImageFormats {
  largest?: ImageFormat;
  largest_webp?: ImageFormat;
  larger?: ImageFormat;
  larger_webp?: ImageFormat;
  large?: ImageFormat;
  large_webp?: ImageFormat;
  medium?: ImageFormat;
  medium_webp?: ImageFormat;
  small?: ImageFormat;
  small_webp?: ImageFormat;
}
